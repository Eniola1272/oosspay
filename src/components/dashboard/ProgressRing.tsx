// TODO: SVG circular progress ring for savings target visualization
export function ProgressRing({ progress }: { progress: number }) {
  void progress;
  return <div className="relative inline-flex" />;
}
